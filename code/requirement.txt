# Core interactive pipeline (Streamlit app)
streamlit>=1.35
biopython>=1.83
selenium>=4.23
beautifulsoup4>=4.12
requests>=2.32
numpy>=2.0
pandas>=2.2

# Excel I/O — used by dataset/code.py (xlsxwriter engine) and pandas.read_excel
# (openpyxl engine) across the pipeline and dataset scripts
openpyxl>=3.1
xlsxwriter>=3.2

# Plotting / stats — used by dataset/generate_figures.py to reproduce the
# manuscript figures (incl. hierarchical clustering for Figure 2) from the
# deposited dataset
matplotlib>=3.9
scipy>=1.11
