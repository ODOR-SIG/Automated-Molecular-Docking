# requirements.txt — OdorSig
#
# Provenance: pinned to THE VERIFIED WORKING ENVIRONMENT USED TO RUN OdorSig
# (pip freeze provided by the maintainer, 2026-07).
#
# Python version: exact Python-version parity is NOT guaranteed across the
# machines in this project's history (the original Windows dataset-generation
# laptop; the later WSL/Ubuntu verified environment; and CI). These pins target
# the Python 3.11-3.13 range. Note that the pinned numpy (2.4.6) and pandas
# (3.0.3) require Python >= 3.11, so 3.11 is the effective minimum for this
# pinned set. scipy is pinned to 1.17.1 — the newest scipy that supports
# Python 3.11 — rather than 1.18.0, which requires Python >= 3.12 and would
# fail to install on CI's Python 3.11.
#
# Scope: this file lists ONLY the packages actually imported by the repository
# source code. Transitive dependencies are resolved automatically by pip and are
# intentionally not pinned here. External command-line tools (AutoDock Vina,
# Open Babel, PyMOL) are NOT Python packages and are recorded separately in
# environment_lock.txt.

# --- Core pipeline (dataset/code.py, code/app.py, code/Automation_code/*) ---
biopython==1.87
beautifulsoup4==4.14.3
requests==2.34.2
selenium==4.44.0
numpy==2.4.6
pandas==3.0.3
streamlit==1.58.0
openpyxl==3.1.5          # pandas Excel READ engine (pd.read_excel)
xlsxwriter==3.2.9        # pandas Excel WRITE engine (pd.ExcelWriter(engine="xlsxwriter"))

# --- Figure regeneration only (dataset/generate_figures.py) ---
# Used to reproduce the manuscript's dataset-level figures from
# Final_Docking_Results.xlsx; not required to run the docking pipeline itself.
scipy==1.17.1            # scipy.cluster.hierarchy — Fig. 2 clustering (newest scipy for Py3.11; 1.18.0 needs Py>=3.12)
matplotlib==3.11.0       # figure rendering in dataset/generate_figures.py
